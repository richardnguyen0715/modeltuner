TRAINING:
  CHECKPOINT_PATH: ViVQA-Models
  LEARNING_RATE: 2.0e-5
  WEIGHT_DECAY: 1.0e-3
  EPOCHS: 100
  BATCH_SIZE: 8
  WORKERS: 0
  METRIC_BEST: em
  PATIENCE: 5

MODEL:
  NAME: bartpho_beit_generate
  DEVICE: cuda

  VISION_EMBEDDING:
    PRETRAINED_NAME: microsoft/beit-base-patch16-224-pt22k-ft22k
    D_PRETRAINED_FEATURE: 768

  TEXT_EMBEDDING:
    PRETRAINED_NAME: vinai/bartpho-word-base

  TOKENIZER:
    PADDING: max_length
    MAX_INPUT_LENGTH: 40
    MAX_TARGET_LENGTH: 10
    TRUNCATION: True
    RETURN_ATTENTION_MASK: True

  GENERATOR:
    MAX_LENGTH: 12
    MIN_LENGTH: 1
    NUM_BEAMS: 4
    LENGTH_PENALTY: 1.5
    NO_REPEAT_NGRAM_SIZE: 3
    EARLY_STOPPING: True
